{
    "authenticationMethod": "route",
    "routes": [
      {
        "source": "^/service/(.*)$",
        "authenticationType": "xsuaa",
        "scope": "$XSAPPNAME.Admin",
        "destination": "srv"
      }
    ]
  }
  